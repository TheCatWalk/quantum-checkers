import Phaser from 'phaser';

// Win/lose screen — implemented in step 6.
export class GameOverScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameOverScene' });
  }

  create(): void {}
}
